[package]
name = "zyvor-qa-rust"
version = "0.0.0"
edition = "2021"
description = "High-throughput artifact processing for Zyvor QA Agent (Phase 4)"

[dependencies]
# Phase 4: image diff, log parsing
