"""Autofix agent — Phase 4 stub."""


def suggest_fixes(failure_output: str) -> list[str]:
    """Return suggested selector fixes. Phase 4 implementation."""
    return ["Autofix not implemented (Phase 4)"]
