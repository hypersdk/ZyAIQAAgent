apiVersion: apps/v1
kind: Deployment
metadata:
  name: zyvor-qa-webhook
  labels:
    app: zyvor-qa-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zyvor-qa-agent
      component: webhook
  template:
    metadata:
      labels:
        app: zyvor-qa-agent
        component: webhook
    spec:
      serviceAccountName: zyvor-qa
      containers:
        - name: zyvor-qa
          image: zyvor-qa-agent:latest
          command: ["zyvor-qa", "serve", "--port", "8080", "--host", "0.0.0.0"]
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: zyvor-qa-config
            - secretRef:
                name: zyvor-qa-secrets
          env:
            - name: ZYVOR_API_TOKENS_FILE
              value: /app/secrets/api-tokens.json
          # generous timeouts: in-pod Playwright runs briefly saturate the CPU
          # limit and must not fail probes (1s default timeout kills the pod)
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 8
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 8
            failureThreshold: 9
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              # headroom for dashboard-triggered Playwright runs (Chromium)
              memory: "4Gi"
              cpu: "2000m"
          volumeMounts:
            - name: reports
              mountPath: /app/reports
            - name: api-tokens
              mountPath: /app/secrets
              readOnly: true
      volumes:
        - name: reports
          persistentVolumeClaim:
            claimName: zyvor-qa-reports
        - name: api-tokens
          secret:
            secretName: zyvor-qa-secrets
            items:
              - key: api-tokens.json
                path: api-tokens.json
