apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: argus-reports
  labels:
    app: argus
spec:
  # k3s ships the local-path provisioner by default
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
